<button type="button" class="btn btn-primary expanded-button" data-toggle="modal" data-target="#departmentModal">New Duty Entity</button>
<div class="modal fade" id="departmentModal" role="dialog">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <span class="modal-title">Add Duty Entity</span>
        <button type="button" class="close" data-dismiss="modal">&times;</button>
      </div>
      <div class="modal-body">
        <div class="form-group">
          <%= form_for @duty_entity, method: :post do |f| %>
            <div class="row button-inline-align">
              <div class="col-sm-6">
                <label for="name">Duty Entity Name:</label>
                <%= f.text_field :name, class: 'form-control' %>
              </div>
              <div class="col-sm-6">
                <%= f.submit "Add Duty Entity", class: 'btn btn-primary expanded-button' %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
