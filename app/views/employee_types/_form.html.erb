<button type="button" class="btn btn-primary expanded-button" data-toggle="modal" data-target="#designationModal">Add Designation</button>
<div class="modal fade" id="designationModal" role="dialog">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <span class="modal-title">Add Designation</span>
        <button type="button" class="close" data-dismiss="modal">&times;</button>
      </div>
      <div class="modal-body">
        <div class="form-group">
          <%= form_for @employee_type, method: 'post' do |f| %>
            <div class="row button-inline-align">
              <div class="col-sm-6">
                <label for="name">Designation Name:</label>
                <%= f.text_field :name, class: 'form-control' %>
              </div>
              <div class="col-sm-6">
                <%= f.submit "Add Designation Type", class: 'btn btn-primary expanded-button' %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
